## Module 6 — Engineering tooling

Shipping quality software depends on **repeatable environments**, **tests**, and **automation**—not only heroics in the IDE.

### Read in the repo

- [devcontainer README](command:engineeringTutorial.openDevcontainerReadme)

Skim:

- `education/engineering-tooling/test/` — how different test layers are documented (unit, integration, smoke, …).
- `education/engineering-tooling/github/copilot-skills/` — how teams encode recurring procedures for assistants and CI.

### Practice in VS Code

1. **Terminal** (`Ctrl+`` `): run a real script from your own project (tests, lint, build).
2. **Problems** (`Ctrl+Shift+M`): fix until the panel is clean—treat diagnostics like a mini CI.

### SE takeaway

**Devcontainer / CI / skills** are three forms of the same thing: **encoded environment + process**.
